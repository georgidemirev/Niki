import React from "react"
import { Text, View } from "react-native"

const RegisterScreen = () => {
	return (
		<View style={{flex: 1, alignItems: 'center', justifyContent: 'center'}}>
			<Text>Register Sceen</Text>
		</View>
	)
}

export default RegisterScreen